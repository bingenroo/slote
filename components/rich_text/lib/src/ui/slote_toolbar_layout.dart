/// Toolbar layout variants for Slote rich text formatting UIs.
///
/// Kept in `package:rich_text` so the app and example can share a single type,
/// and a future user setting can map to this enum directly.
enum SloteToolbarLayout {
  horizontalScroll,
  verticalScroll,
}

