name: rich_text
description: Placeholder for Slote rich text; see example/ for AppFlowy editor demo.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  appflowy_editor: ^6.2.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

dependency_overrides:
  appflowy_editor:
    path: ../appflowy_editor

flutter:
  uses-material-design: true
